// convert decimal to binary
#include <stdio.h>
#include <math.h>
int main() 
{
    int n, a, sum=0;
    // printf("Enter no of terms: ");
    // scanf("%d", &n);
    // printf("Enter power: ");
    // scanf("%d", &a);
    // for(int i=1; i<=n; i++)
    // {
    //     sum = sum + pow(-1, i + 1) * pow(i, a);
    // }
    //printf("Sum of series=%d", sum);
    a=4;
    int x = ++a + a++ + --a;
    printf("Sum of xs=%d\n", x);
    printf("Sum of a=%d\n", a);
}
